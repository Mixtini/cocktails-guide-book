import React from 'react';

import styled from 'styled-components';

const Recipe = () => {
    return (
        <HeaderText>Recipe</HeaderText>
    );
};

export default Recipe;

const HeaderText = styled.div`
`;