import React from 'react';

import styled from 'styled-components';

const Surfing = () => {
    return (
        <HeaderText>Surfing</HeaderText>
    );
};

export default Surfing;

const HeaderText = styled.div`
`;